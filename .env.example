# Environment variables for FactLens extension
# Copy this to `.env` and update values as needed. Never commit secrets.

# Use mock data (true) or real backend (false)
VITE_USE_MOCKS=true

# Backend API base URL (used when VITE_USE_MOCKS=false)
VITE_API_BASE_URL=http://localhost:8000
